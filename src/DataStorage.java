/**
 * The 'DataStorage' interface represents the 'Program to an interface, not an implementation' principle.
 */
interface DataStorage {
    void save(String data); // Method signature for saving data
}